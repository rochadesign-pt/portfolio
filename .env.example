# Google Analytics 4 — set to your Measurement ID to activate GA4.
# Leave empty to keep GA4 off (Vercel Analytics still works on its own).
# In Vercel: Project → Settings → Environment Variables → VITE_GA_ID
VITE_GA_ID=
